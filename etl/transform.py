def transform_data(data):
    """
    The `transform_data` function in Python simply returns the input data without any
    transformation.

    :param data: The `transform_data` function you provided simply returns the input
    data as it is without any transformation.
    :return: the `transformed_data`, which is a variable holding the same value as the
    input `data`.
    """
    transformed_data = data
    return transformed_data
